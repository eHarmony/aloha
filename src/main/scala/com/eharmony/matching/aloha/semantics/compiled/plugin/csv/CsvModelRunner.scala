package com.eharmony.matching.aloha.semantics.compiled.plugin.csv


object CsvModelRunner {

    /**
      * val parser = new scopt.OptionParser[Config]("scopt") {
      *   head("scopt", "3.x")
      *   opt[Int]('f', "foo") action {
      *     (x, c) => c.copy(foo = x)
      *   } text("foo is an integer property")
      *   opt[File]('o', "out") required() valueName("<file>") action {
      *     (x, c) => c.copy(out = x)
      *   } text("out is a required file property")
      * }
      *
      * @param args
      */
    def main(args: Array[String]) {

    }

    def argParser(args: Array[String]) = {
        // --enum UserStatusProto:a, (a-z+)(\.(a-z+))*

//        val parser = new scopt.OptionParser[Config]("scopt") {
    }
}
