package com.eharmony.aloha.factory

/**
  * Provides conversions from RefInfo to spray-json's JsonFormat.  This allows the library user
  * to omit including a JsonFormat.  Instead, the user supplies a function to perform the conversion.
  */
package object ri2jf {}
