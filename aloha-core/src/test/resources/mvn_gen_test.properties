targetDirectory = ${project.build.directory}

testClassesDirectory = ${project.build.directory}/test-classes

##########################################################################
# Make sure that a property "generated.classes.test" is set in the POM to 
#  
#     ${project.build.directory}/test-classes/generated
#
# Then replace the variable def below with
# 
#     testGeneratedClasses = ${generated.classes.test}
#
##########################################################################
testGeneratedClasses = ${project.build.directory}/test-classes/generated

testDirectory = ${project.basedir}/src/test
