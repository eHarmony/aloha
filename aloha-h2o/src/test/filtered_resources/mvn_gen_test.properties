# These files are filled in by SBT to be used by FileLocations.

targetDirectory = ${projectBuildDirectory}
testClassesDirectory = ${projectBuildDirectory}/test-classes

# Can only get this working in the root test classes directory because of serializability test.
testGeneratedClasses = ${projectBuildDirectory}/test-classes
testDirectory = ${scalaTestSource}
